Usage: source commands
Helper functions for the lib

This script needs to be sourced to work properly
The avaliable functions are listed below

prefix {PREFIX}
    Sets the bot prefix variable
    PREFIX: A string having one or more prefixes

xcommand {FUNC} {NAME} {CTX_STRING}
    Defines a command
    FUNC: The name of the function
    NAME: A string having one or more names
    CTX_STRING: A jq query for the CTX variable,
        needs to return values separated by lines
        an example would be '.channel_id,.id'
