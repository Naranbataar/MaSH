#!/bin/bash

if [ -z "$1" ]; then
        echo "Usage: ws-send {OP_CODE} [SHARD]" >&2
        echo "Sends the JSON data from the STDIN to the gateway, default by SHARD 0"
        exit 1
fi

read -r TEXT
PIPE="$MASH_STATUS_DIR/ws_pipe_${2:-0}"
echo "{\"op\":$1,\"d\":$TEXT}" > "$PIPE" 
