Usage: invite {COMMAND}
Interacts with invite endpoints

Every command accept a string of JSON on STDIN
Avaliable commands with their respective JSON keys

get: {code}
    code: (string) the code of the invite

new: {channel} [max_age] [max_uses] [temporary] [unique]
    channel: (snowflake) the id of the channel
    max_age: (integer) seconds for the invite to be valid, 0 for forever
             (default: 86400)
    max_uses: (integer) max number of uses, 0 for unlimited (default: 0)
    temporary: (boolean) if the invite only grants temporary membership
               (default:false)
    unique: (boolean) if true, the invite can be used only one time
            (default: false)

del: {code}
    code: (string) the code of the invite

list_ch: {id}
    id: (snowflake) the id of the channel

list_g: {id}
    id: (snowflake) the id of the guild
