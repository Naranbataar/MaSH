Usage: integration {COMMAND}
Interacts with guild integration endpoints

Every command accept a string of JSON on STDIN
Avaliable commands with their respective JSON keys

get: {guild}
    guild: (snowflake) the id of the guild

new: {id} {guild} {type}
    id: (snowflake) the id of the integration
    guild: (snowflake) the id of the guild
    type: (string) the type of the integration

edit: {id} {guild} [expire_behavior] [expire_grace_period]
[enable_emoticons]
    id: (snowflake) the id of the integration
    guild: (snowflake) the id of the guild
    expire_behavior: (integer) the behavior when an
        integration subscription lapses
    expire_grace_period: (integer) period in seconds
        where the integration will ignore lapsed
        subscriptions
    enable_emoticons (boolean) whether emoticons should be
        synced for this integration

del: {id} {guild}
    id: (snowflake) the id of the integration
    guild: (snowflake) the id of the guild

sync: {id} {guild}
    id: (snowflake) the id of the integration
    guild: (snowflake) the id of the guild
